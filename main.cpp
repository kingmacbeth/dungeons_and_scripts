int main(){
    int x, y, z;
    float ix, t, casa, preco;
    char letra, l, operacao;
    double temperatura, juros, nota;
    
    x = 10;
    y = 20;
    z = x;
    
    t = x + y;
    preco = y * 2 + 3;
    temperatura = (x + 2) / (z - 6);
}
